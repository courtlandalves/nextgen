# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
common: &common
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 30

development:
  <<: *common
  host: localhost
  database: nextgen_development
  username: 

  
